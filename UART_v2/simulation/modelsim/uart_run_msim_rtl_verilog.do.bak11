transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vlog -vlog01compat -work work +incdir+D:/UART\ and\ MatMul/UART_v2 {D:/UART and MatMul/UART_v2/buadrate.v}
vlog -vlog01compat -work work +incdir+D:/UART\ and\ MatMul/UART_v2 {D:/UART and MatMul/UART_v2/baudrate_tb.v}

